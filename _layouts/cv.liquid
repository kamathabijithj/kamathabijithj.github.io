---
layout: default
---
{% unless site.data.resume %}
  <div class="post">
    <header class="post-header">
      <h1 class="post-title">
        {{ page.title }}
        {% if page.cv_pdf %}
          <a
            href="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url}}"
            target="_blank"
            rel="noopener noreferrer"
            class="cv-pdf-link"
            title="Download PDF CV"
            ><i class="fa-solid fa-file-pdf"></i>
          </a>
        {% endif %}
      </h1>
      {% if page.description %}
        <p class="post-description">{{ page.description }}</p>
      {% endif %}
    </header>

    <article>
      <div class="cv-modern">
        {% for entry in site.data.cv %}
          <a class="anchor" id="{{ entry.title }}"></a>
          <div class="cv-section-card">
            <div class="cv-section-header">
              <h3 class="cv-section-title">
                <i class="cv-section-icon {% if entry.title == 'General Information' %}fas fa-user{% elsif entry.title == 'Education' %}fas fa-graduation-cap{% elsif entry.title == 'Experience' %}fas fa-briefcase{% elsif entry.title == 'Publications' %}fas fa-file-alt{% elsif entry.title == 'Awards' %}fas fa-trophy{% elsif entry.title == 'Skills' %}fas fa-code{% else %}fas fa-circle{% endif %}"></i>
                {{ entry.title }}
              </h3>
            </div>
            <div class="cv-section-content">
              {% if entry.type == 'list' %}
                {% include cv/list.liquid %}
              {% elsif entry.type == 'map' %}
                {% include cv/map.liquid %}
              {% elsif entry.type == 'nested_list' %}
                {% include cv/nested_list.liquid %}
              {% elsif entry.type == 'time_table' %}
                {% include cv/time_table.liquid %}
              {% elsif entry.type == 'list_groups' %}
                {% include cv/list_groups.liquid %}
              {% else %}
                {{ entry.contents }}
              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  </div>
{% else %}
  <div class="post">
    <header class="post-header">
      <h1 class="post-title">
        {{ page.title }}
        {% if page.cv_pdf %}
          <a
            href="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url}}"
            target="_blank"
            rel="noopener noreferrer"
            class="cv-pdf-link"
            title="Download PDF CV"
            ><i class="fa-solid fa-file-pdf"></i>
          </a>
        {% endif %}
      </h1>
      {% if page.description %}
        <p class="post-description">{{ page.description }}</p>
      {% endif %}
    </header>

    <article>
      <div class="cv-modern">
        {% for data in site.data.resume %}
          {% if site.jsonresume and site.jsonresume.size > 0 %}
            {% unless site.jsonresume contains data[0] %}
              {% continue %}
            {% endunless %}
          {% endif %}
          {% if data[0] == 'meta' or data[1].size == 0 %} {% continue %} {% endif %}
          <a class="anchor" id="{{ data[0] }}"></a>
          <div class="cv-section-card">
            <div class="cv-section-header">
              <h3 class="cv-section-title">
                <i class="cv-section-icon {% if data[0] == 'basics' %}fas fa-user{% elsif data[0] == 'education' %}fas fa-graduation-cap{% elsif data[0] == 'work' %}fas fa-briefcase{% elsif data[0] == 'publications' %}fas fa-file-alt{% elsif data[0] == 'awards' %}fas fa-trophy{% elsif data[0] == 'skills' %}fas fa-code{% elsif data[0] == 'volunteer' %}fas fa-hands-helping{% elsif data[0] == 'projects' %}fas fa-project-diagram{% else %}fas fa-circle{% endif %}"></i>
                {{ data[0] | capitalize }}
              </h3>
            </div>
            <div class="cv-section-content">
              {% case data[0] %}
                {% when 'basics' %}
                  {% include resume/basics.liquid %}
                {% when 'education' %}
                  {% include resume/education.liquid %}
                {% when 'work' %}
                  {% include resume/work.liquid %}
                {% when 'volunteer' %}
                  {% include resume/volunteer.liquid %}
                {% when 'projects' %}
                  {% include resume/projects.liquid %}
                {% when 'awards' %}
                  {% include resume/awards.liquid %}
                {% when 'skills' %}
                  {% include resume/skills.liquid %}
                {% when 'publications' %}
                  {% include resume/publications.liquid %}
                {% when 'languages' %}
                  {% include resume/languages.liquid %}
                {% when 'interests' %}
                  {% include resume/interests.liquid %}
                {% when 'certificates' %}
                  {% include resume/certificates.liquid %}
                {% when 'references' %}
                  {% include resume/references.liquid %}
                {% else %}

              {% endcase %}
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  </div>
{% endunless %}

<style>
/* Swiss Design CV Styling */
.post-header {
  margin-bottom: 3rem;
}

.post-title {
  font-size: 2.1rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  margin-bottom: 1rem;
  position: relative;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: none;
  padding-bottom: 0;
}

.post-title::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -0.75rem;
  width: 3rem;
  height: 2px;
  background: var(--global-theme-color);
}

.cv-pdf-link {
  font-size: 1.5rem;
  color: var(--global-theme-color);
  transition: color 0.2s ease;
  text-decoration: none;
}

.cv-pdf-link:hover {
  color: var(--global-text-color);
  text-decoration: none;
}

.post-description {
  font-size: 0.9rem;
  color: var(--global-text-color-light);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  margin-bottom: 0;
}

.cv-modern {
  display: flex;
  flex-direction: column;
  gap: 4rem;
  max-width: 54rem;
  margin: 0 auto;
}

.cv-section-card {
  background-color: transparent;
  border: none;
  padding: 0;
  margin-bottom: 0;
}

.cv-section-header {
  margin-bottom: 2rem;
  border-bottom: 1px solid var(--global-divider-color);
  padding-bottom: 0.5rem;
}

.cv-section-title {
  font-size: 1rem;
  letter-spacing: 0.18em;
  margin: 0;
  text-transform: uppercase;
  font-weight: 600;
  color: var(--global-text-color);
  display: flex;
  align-items: center;
}

.cv-section-icon {
  display: none;
}

/* CV Timeline Styles - Swiss Design */
.cv-timeline {
  position: relative;
  padding-left: 0;
}

.cv-timeline-item {
  position: relative;
  padding-bottom: 2.5rem;
  border-left: 1px solid var(--global-divider-color);
  padding-left: 1.5rem;
  margin-left: 0;
}

.cv-timeline-item:last-child {
  border-left: 1px solid var(--global-divider-color);
  padding-bottom: 0;
}

.cv-timeline-marker {
  display: none;
}

.cv-item-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 0.5rem;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.cv-item-title {
  font-weight: 500;
  font-size: 1rem;
  color: var(--global-text-color);
  margin: 0;
  letter-spacing: 0.02em;
}

.cv-item-subtitle {
  font-size: 0.9rem;
  color: var(--global-text-color);
  margin-bottom: 0.5rem;
  font-weight: 400;
}

.cv-year-badge {
  background-color: transparent;
  color: var(--global-text-color-light);
  padding: 0;
  border-radius: 0;
  font-size: 0.75rem;
  font-weight: 400;
  white-space: nowrap;
  text-transform: uppercase;
  letter-spacing: 0.12em;
}

.cv-timeline-content {
  position: relative;
}

.cv-timeline-content::before {
  content: "";
  position: absolute;
  left: -1.85rem;
  top: 0.3rem;
  width: 0.4rem;
  height: 0.4rem;
  background-color: var(--global-theme-color);
  border-radius: 0;
}

.cv-details {
  margin-top: 0.5rem;
}

.cv-title {
  font-size: 1rem;
  font-weight: 500;
  letter-spacing: 0.02em;
  margin-bottom: 0.5rem;
  color: var(--global-text-color);
}

.cv-institution-info {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
  margin-bottom: 0.75rem;
}

.institution-item,
.department-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.85rem;
  color: var(--global-text-color-light);
}

.institution-item i,
.department-item i {
  font-size: 0.7rem;
  color: var(--global-theme-color);
  opacity: 0.7;
}

.cv-main-description {
  font-size: 0.9rem;
  color: var(--global-text-color-light);
  line-height: 1.6;
  margin-bottom: 0.75rem;
}

.cv-description-list {
  padding-left: 1rem;
  margin-bottom: 0.5rem;
  list-style: none;
}

.cv-description-item {
  margin-bottom: 0.4rem;
  font-size: 0.9rem;
  color: var(--global-text-color-light);
  position: relative;
  padding-left: 1rem;
}

.cv-description-item::before {
  content: "–";
  position: absolute;
  left: 0;
  color: var(--global-theme-color);
}

.cv-sub-list {
  padding-left: 1rem;
  margin-top: 0.3rem;
  list-style: none;
}

.cv-sub-list li {
  font-size: 0.85rem;
  margin-bottom: 0.25rem;
  position: relative;
  padding-left: 0.75rem;
}

.cv-sub-list li::before {
  content: "·";
  position: absolute;
  left: 0;
  color: var(--global-text-color-light);
}

.location-info {
  display: flex;
  align-items: center;
  gap: 0.35rem;
  margin-top: 0.25rem;
  font-size: 0.7rem;
  color: var(--global-text-color-light);
  text-transform: none;
  letter-spacing: 0.06em;
}

.location-info i {
  font-size: 0.65rem;
  opacity: 0.7;
}

.cv-links {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-top: 0.75rem;
}

.cv-link-button {
  border-radius: 0;
  border: 1px solid var(--global-divider-color);
  background: transparent;
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  padding: 0.15rem 0.6rem;
  color: var(--global-text-color) !important;
  text-decoration: none !important;
  transition: all 0.2s ease;
  display: inline-block;
}

.cv-link-button:hover {
  border-color: var(--global-theme-color);
  color: var(--global-theme-color) !important;
  background: transparent;
  text-decoration: none !important;
  transform: none;
}

/* CV List Groups Styles - Swiss Design */
.cv-list-groups {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2.5rem;
}

.cv-group-section {
  background-color: transparent;
  border-radius: 0;
  padding: 0;
  border: none;
  transition: none;
}

.cv-group-section:hover {
  transform: none;
  box-shadow: none;
  border-color: transparent;
}

.cv-group-header {
  display: flex;
  align-items: center;
  margin-bottom: 1.25rem;
  padding-bottom: 0.5rem;
  border-bottom: 1px solid var(--global-divider-color);
}

.cv-group-icon {
  display: none;
}

.cv-group-title {
  color: var(--global-text-color);
  font-weight: 600;
  font-size: 0.85rem;
  margin: 0;
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

.cv-group-items {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.cv-group-item {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 0.75rem 0;
  background-color: transparent;
  border-radius: 0;
  border-bottom: 1px solid var(--global-divider-color);
  transition: none;
}

.cv-group-item:last-child {
  border-bottom: none;
}

.cv-group-item:hover {
  border-color: var(--global-divider-color);
  transform: none;
}

.cv-item-main {
  flex: 1;
}

.cv-item-name {
  font-weight: 500;
  color: var(--global-text-color);
  margin-bottom: 0.3rem;
  font-size: 0.9rem;
  letter-spacing: 0.02em;
}

.cv-item-details {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  align-items: center;
}

.cv-item-level,
.cv-item-school,
.cv-item-time {
  font-size: 0.75rem;
  color: var(--global-text-color-light);
  background-color: transparent;
  padding: 0;
  border-radius: 0;
  border: none;
  text-transform: uppercase;
  letter-spacing: 0.12em;
}

.cv-item-level {
  background: transparent;
  color: var(--global-theme-color);
  font-weight: 500;
}

.cv-item-link {
  margin-left: 1rem;
  flex-shrink: 0;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .cv-year-badge {
    min-width: auto;
    max-width: 100%;
  }
  
  .cv-list-groups {
    grid-template-columns: 1fr;
  }
  
  .cv-group-item {
    flex-direction: column;
    align-items: stretch;
    gap: 0.5rem;
  }
  
  .cv-item-link {
    margin-left: 0;
    align-self: flex-start;
  }
  
  .cv-item-details {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.25rem;
  }
}

/* Dark mode enhancements */
html[data-theme="dark"] .cv-group-section {
  background-color: transparent;
  border-color: transparent;
}

html[data-theme="dark"] .cv-group-section:hover {
  box-shadow: none;
}

html[data-theme="dark"] .cv-group-item {
  background-color: transparent;
  border-color: var(--global-divider-color);
}

html[data-theme="dark"] .cv-item-level,
html[data-theme="dark"] .cv-item-school,
html[data-theme="dark"] .cv-item-time {
  background-color: transparent;
  border-color: transparent;
}

/* CV Clean List Styles - Swiss Design */
.cv-clean-list {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.cv-list-item {
  display: flex;
  align-items: center;
  padding: 0.75rem 0;
  background-color: transparent;
  border-radius: 0;
  border-bottom: 1px solid var(--global-divider-color);
  transition: none;
}

.cv-list-item:last-child {
  border-bottom: none;
}

.cv-list-item:hover {
  background-color: transparent;
  border-color: var(--global-divider-color);
  transform: none;
}

.cv-list-bullet {
  color: var(--global-theme-color);
  font-size: 0.3rem;
  margin-right: 1rem;
  opacity: 0.8;
}

.cv-list-content {
  flex: 1;
  color: var(--global-text-color);
  font-size: 0.9rem;
  line-height: 1.5;
  letter-spacing: 0.01em;
}

/* CV Info Grid Styles - Swiss Design */
.cv-info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2.5rem;
}

.cv-info-item {
  background-color: transparent;
  border-radius: 0;
  padding: 0;
  border: none;
  transition: none;
  position: relative;
  overflow: visible;
  border-left: 1px solid var(--global-divider-color);
  padding-left: 1rem;
}

.cv-info-item::before {
  display: none;
}

.cv-info-item:hover {
  transform: none;
  box-shadow: none;
  border-color: var(--global-theme-color);
}

.cv-info-label {
  display: flex;
  align-items: center;
  margin-bottom: 0.75rem;
  color: var(--global-text-color);
  font-weight: 600;
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

.cv-info-icon {
  display: none;
}

.cv-info-value {
  color: var(--global-text-color);
  font-size: 0.9rem;
  line-height: 1.6;
  margin-bottom: 0.5rem;
  letter-spacing: 0.01em;
}

.cv-info-links {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin-top: 0.75rem;
}

/* CV Nested Sections Styles - Swiss Design */
.cv-nested-sections {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
}

.cv-nested-section {
  background-color: transparent;
  border-radius: 0;
  padding: 0;
  border: none;
  transition: none;
}

.cv-nested-section:hover {
  transform: none;
  box-shadow: none;
  border-color: transparent;
}

.cv-nested-header {
  display: flex;
  align-items: center;
  margin-bottom: 1.25rem;
  padding-bottom: 0.5rem;
  border-bottom: 1px solid var(--global-divider-color);
}

.cv-nested-icon {
  display: none;
}

.cv-nested-title {
  color: var(--global-text-color);
  font-weight: 600;
  font-size: 0.85rem;
  margin: 0;
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

.cv-nested-items {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.cv-nested-item {
  display: flex;
  align-items: flex-start;
  padding: 0.5rem 0;
  transition: none;
  border-bottom: 1px solid var(--global-divider-color);
}

.cv-nested-item:last-child {
  border-bottom: none;
}

.cv-nested-item:hover {
  transform: none;
}

.cv-nested-bullet {
  color: var(--global-theme-color);
  font-size: 0.3rem;
  margin-right: 1rem;
  margin-top: 0.4rem;
  opacity: 0.8;
}

.cv-nested-content {
  flex: 1;
  color: var(--global-text-color);
  font-size: 0.9rem;
  line-height: 1.6;
  letter-spacing: 0.01em;
}

/* Enhanced responsive design for all CV components */
@media (max-width: 768px) {
  .cv-info-grid {
    grid-template-columns: 1fr;
  }
  
  .cv-info-item,
  .cv-nested-section {
    padding: 0;
  }
  
  .cv-list-item {
    padding: 0.5rem 0;
  }
  
  .cv-nested-header {
    margin-bottom: 0.75rem;
  }
}

/* Dark mode compatibility for new components */
html[data-theme="dark"] .cv-list-item {
  background-color: transparent;
  border-color: var(--global-divider-color);
}

html[data-theme="dark"] .cv-list-item:hover {
  background-color: transparent;
  box-shadow: none;
}

html[data-theme="dark"] .cv-info-item {
  background-color: transparent;
  border-color: var(--global-divider-color);
}

html[data-theme="dark"] .cv-info_item:hover {
  box-shadow: none;
  border-color: var(--global-theme-color);
}

html[data-theme="dark"] .cv-nested-section {
  background-color: transparent;
  border-color: transparent;
}

html[data-theme="dark"] .cv-nested-section:hover {
  box-shadow: none;
}
</style>
